<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.formation</groupId>
  <artifactId>mcp-chat-java</artifactId>
  <name>MCP Chat (Java)</name>
  <version>1.0.0</version>
  <description>Conversion Java de l'exercice "MCP Chat" (Introduction to Model Context Protocol).
        Client CLI de chat Anthropic avec integration MCP (resources, prompts, tools).</description>
  <build>
    <finalName>mcp-chat</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven-compiler-plugin.version}</version>
        <configuration>
          <release>21</release>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>${exec-maven-plugin.version}</version>
        <configuration>
          <mainClass>com.formation.mcpchat.Main</mainClass>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>${maven-dependency-plugin.version}</version>
        <executions>
          <execution>
            <id>build-classpath</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>build-classpath</goal>
            </goals>
            <configuration>
              <outputFile>${project.build.directory}/classpath.txt</outputFile>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven-shade-plugin.version}</version>
        <executions>
          <execution>
            <id>shade-mcp-server</id>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <finalName>mcp-chat-server</finalName>
              <transformers>
                <transformer>
                  <mainClass>com.formation.mcpchat.McpServerApp</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <properties>
    <maven-dependency-plugin.version>3.7.1</maven-dependency-plugin.version>
    <slf4j.version>2.0.16</slf4j.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <anthropic-java.version>2.40.1</anthropic-java.version>
    <mcp-java-sdk.version>2.0.0</mcp-java-sdk.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven-shade-plugin.version>3.6.0</maven-shade-plugin.version>
    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
    <jline.version>3.26.3</jline.version>
    <maven.compiler.target>21</maven.compiler.target>
    <dotenv-java.version>3.2.0</dotenv-java.version>
    <exec-maven-plugin.version>3.2.0</exec-maven-plugin.version>
  </properties>
</project>
